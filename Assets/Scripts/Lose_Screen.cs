using UnityEngine;

public class Lose_Screen : MonoBehaviour
{

    float player1_hp; // HP Placeholder
    float player2_hp; // HP Placeholder

    float player1_points; // Point placeholder 
    float player2_points; // Point placeholder

    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }

    private void LoseScreen()
    {
        if(player1_hp == 0 || player2_hp == 0)
        {
            
        }
    }


}
